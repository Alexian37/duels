/**
 * Reference module describing characters (but not the cards)
 * DEF is an array, containing one entry per character. Each entry has as many lines as versions (at least one for the base version)
 * 
 * @module characters
 */

define({

    DEF: [

        [{ version_name: "Original", setup: [SPEED_TOKEN, 3], hit: GAIN_SPEED_PER_DAMAGE_LIMIT5, bid: SPEED_TOKEN_FOR_INIT }],

        [{ version_name: "Original" }]

    ]


});